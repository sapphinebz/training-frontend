import { LoadImageNameDirective } from './load-image-name.directive';

describe('LoadImageNameDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadImageNameDirective();
    expect(directive).toBeTruthy();
  });
});
